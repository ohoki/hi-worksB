<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.worksb.hi.mycalendar.mapper.ToDoListMapper">
	<!-- 단건조회 -->
	<select id="selectTdl" resultType="ToDoListVO" parameterType="int">
		SELECT list_id,             
			   list_title,   
			   list_regdate,           
			   member_id,     
			   apply_date   
		FROM to_do_list
		WHERE list_id = #{listId}
	</select>
	<!-- 전체조회 -->
	<select id="selectAllTdl" resultType="ToDoListVO" parameterType="String">
		SELECT list_id,             
			   list_title,   
			   list_regdate,           
			   member_id,     
			   apply_date 
		FROM to_do_list
		WHERE member_id = #{memberId}
	</select>
	<!-- to do list 등록 -->
	<insert id="insertPsche" parameterType="ToDoListVO">
		INSERT INTO to_do_list(
							   list_id
							   , list_title
							   , list_regdate
							   , member_id
							   , apply_date
							   )
		VALUES (#{listId}
				, #{listTitle}
				, sysdate
				, #{memberId}
				, #{applyDate}
				)
	</insert>
	<!-- to do list 수정-->
	
	<!-- to do list 삭제 -->
</mapper>